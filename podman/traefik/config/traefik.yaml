# yaml-language-server: $schema=https://json.schemastore.org/traefik-v3.json
api:
  dashboard: true

experimental:
  plugins:
    traefik-oidc-auth:
      moduleName: "github.com/sevensolutions/traefik-oidc-auth"
      version: "v0.17.0"

certificatesResolvers:
  technitium:
    acme:
      email: "schaermu+acme@pm.me"
      storage: "/certs/acme_technitium.json"
      dnsChallenge:
        provider: rfc2136
        propagation:
          disableANSChecks: true

entryPoints:
  web:
    address: ":8080"
    http:
      redirections:
        entryPoint:
          to: "websecure"
          scheme: "https"

  websecure:
    address: ":8443"
    transport:
      respondingTimeouts:
        readTimeout: 0s
        idleTimeout: 600s
        writeTimeout: 600s
    http:
      tls:
        certResolver: technitium
        domains:
          - main: lab.schaermu.ch
            sans:
              - "*.lab.schaermu.ch"
      middlewares:
        - default@file

global:
  checkNewVersion: true
  sendAnonymousUsage: false

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    watch: true
    exposedByDefault: false
    defaultRule: 'Host(`{{ index .Labels "com.docker.compose.service" }}.lab.schaermu.ch`)'
  file:
    filename: "/etc/traefik/dynamic.conf.yml"
    watch: true
  providersThrottleDuration: 10s

log:
  level: DEBUG

metrics:
  prometheus: {}